paths:
  contracts: contracts

networks:
  default: studionet
  studionet:
    default_wait_interval: 5000
    default_wait_retries: 120
  localnet:
    default_wait_interval: 3000
    default_wait_retries: 120
